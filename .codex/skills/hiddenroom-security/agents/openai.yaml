interface:
  display_name: "Hidden Room Security"
  short_description: "Auth, grants, RLS, RPCs, and secrets"
  default_prompt: "Use $hiddenroom-security to audit authorization end to end, including grants, views, RPCs, service-role functions, and negative role tests."

policy:
  allow_implicit_invocation: true
