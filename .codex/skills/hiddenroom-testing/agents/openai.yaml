interface:
  display_name: "Hidden Room Testing"
  short_description: "Local role, RLS, privilege, and UI QA"
  default_prompt: "Use $hiddenroom-testing to verify Hidden Room locally with synthetic role fixtures, automated RLS/grant negatives, and affected UI flows."

policy:
  allow_implicit_invocation: true
