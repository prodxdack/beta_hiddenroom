interface:
  display_name: "Hidden Room Debian Server"
  short_description: "Debian host, Cloudflare, Docker, agents"
  default_prompt: "Use $hiddenroom-debian-server to inspect MysAuth production securely and read-only by default; require explicit approval before any remote change."

policy:
  allow_implicit_invocation: true
